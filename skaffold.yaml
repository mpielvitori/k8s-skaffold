apiVersion: skaffold/v2beta6
kind: Config
metadata:
  name: k-s-skaffold
build:
  artifacts:
  - image: nodeops
    docker:
      dockerfile: Dockerfile
    context: app
  - image: mongodb
    context: mongodb
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - .k8s/*.yaml
